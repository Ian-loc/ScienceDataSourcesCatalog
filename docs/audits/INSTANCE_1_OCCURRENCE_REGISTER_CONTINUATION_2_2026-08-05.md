# Continuação 2 do registro cumulativo de ocorrências — Instância 1

Horários humanos usam `America/Sao_Paulo`.

| ID | Data e hora | Round | Entidade/arquivo | Categoria | Descrição e evidência | Severidade | Estado | Correção/verificação | Risco residual |
|---|---|---:|---|---|---|---|---|---|---|
| I1-20260805-025 | 05/08/2026 13:30 BRT | Dynamic World — reconciliação operacional normalizada | `DD000017`, `DD000019`; scripts de enriquecimento e validação | força da evidência operacional e proveniência | A primeira implementação transformava a classificação do registro WRI e do repositório de software em `access_status=working` com timestamp de teste, embora o contrato não registrasse teste operacional equivalente ao catálogo Earth Engine e ao visualizador. | high | corrected | Criados pós-processamento idempotente e validador estrito. `DD000017` e `DD000019` permanecem `unknown`, sem `last_access_tested_at`; `DD000016` e `DD000018` preservam estado testado. CI executa enriquecimento, correção duas vezes e ambos os validadores. | Teste vivo autenticado do Earth Engine não é executado no CI e continua explicitamente não afirmado. |
| I1-20260805-026 | 05/08/2026 13:39 BRT | CI #304 — reconciliação do ativo canônico Dynamic World | `scripts/enrich_instance1_dynamic_world_operational.py`; `reconcile_asset()` | defeito de parametrização SQL e chamada de validação | O `INSERT` do ativo `AS-DW-EE-V1` possuía quatro placeholders, mas recebia somente três parâmetros; a chamada final também omitia o argumento `contract` exigido pelo validador. O job `instance1-database` falhou antes de TerraClass e do gate integral. | high | corrected | Incluído `distribution_id` (`pk`) na tupla SQL e corrigida a chamada para `validate(connection, contract)` no commit `7a125d636e66f07ae246ac64de759d2764abc722`. No CI #305, todo o enriquecimento Dynamic World, TerraClass, resolução das distribuições e staging passou até o gate final. | Nenhum risco residual conhecido para a parametrização; o comportamento permanece coberto pela execução PostgreSQL real e pela repetição idempotente no CI. |
| I1-20260805-027 | 05/08/2026 13:43 BRT | CI #305 — gate integral do piloto normalizado | `scripts/validate_instance1_pilot.py`; afirmações de `PR000011` | contrato de validação desatualizado após aprofundamento de evidência | O enriquecimento operacional acrescentou corretamente `required_attribution` e `upstream_notice` ao release Dynamic World, mas o gate integral ainda exigia exatamente 15 afirmações e classificava as duas novas evidências como inesperadas. | high | corrected | O gate foi atualizado para exigir 17 afirmações e incluir explicitamente os dois campos no conjunto obrigatório, no commit `dac1772eb6e77ac0dc1f517d55d920c8d5c6f1d3`. O workflow #306 aprovou integralmente `validate` e `instance1-database`, incluindo o gate final do piloto. | O validador continua estrito: qualquer afirmação ausente ou adicional fora do contrato explícito volta a falhar. |

## Continuidade

As ocorrências estão corrigidas e não bloqueiam o aprofundamento de TerraClass, PRODES ou DETER. O CI integral do PR #54 está verde no SHA `dac1772eb6e77ac0dc1f517d55d920c8d5c6f1d3`. Permanece vedado afirmar disponibilidade operacional testada para endpoints sem evidência direta, promover o banco a autoridade canônica ou mesclar o PR sem os portões científicos e a autorização humana previstos.
